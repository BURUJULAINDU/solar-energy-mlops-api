services:\n  - type: web\n    name: solar-energy-consumption-api\n    runtime: python\n    plan: free\n    buildCommand: pip install -r requirements.txt\n    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT\n